import World from "@/components/scenes/World";

export const metadata = {
  title: "World | KENTO_O",
  description: "You have entered the world.",
};

export default function WorldPage() {
  return <World />;
}
